# OBP v2 — model implementations

TypeScript types for the `khora.obp` graph vocabulary live under this directory.

- **`ts/`** — [`@khoralabs/obp-model`](ts/package.json): `Party`, `Offer`, `Port`, edge shapes, provenance types. Smithy source of truth: [`../spec`](../spec).

Other runtimes can add sibling folders later.
